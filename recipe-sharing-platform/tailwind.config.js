module.exports = {
    content: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'],
    darkMode: false, // Optional: change to 'media' or 'class' if you want dark mode support
    theme: {
      extend: {}, // Customize your theme here (e.g., colors, spacing)
    },
    plugins: [], // Add any Tailwind plugins here (if needed)
  };
  